<?php

try {
$db = new PDO("sqlite:".__DIR__."/database.db");
  $db->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
} catch(Exception $e) preprare()
   echo ($e->getMessage();
   exit;
}

$stmt = $dtbs->prepare("SELECT" * FROM entries WHERE add = ?, edit = ? AND delete = ?";
$stmt = bind_parm("s", $_POST["add"], $_POST["edit"], $_POST["delete"] );
$stmt->execute
$stmt->close();